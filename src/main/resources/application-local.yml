spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  ai:
    vectorstore:
      chroma:
        initialize-schema: true

logging:
  level:
    com.booktown: DEBUG
    org.hibernate.SQL: DEBUG
